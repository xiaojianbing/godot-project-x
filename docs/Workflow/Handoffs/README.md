# Handoffs

这个目录保存跨角色的单一交接文档。

## One Feature, One Handoff

- 每个功能一个 handoff 文件，例如 `air-dash.md`。
- 跨角色共享信息时，以 handoff 为准，不以聊天记录为准。
- 如果角色会话内容与 handoff 冲突，应先修正 handoff。

## Minimal Workflow

1. 复制 `_Template.md`。
2. 按功能名重命名。
3. 每个阶段结束后更新一次。
4. 切换到下一个角色前，把 handoff 粘贴到新会话中。
